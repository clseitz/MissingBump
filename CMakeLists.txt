cmake_minimum_required(VERSION 2.8)
project( FindEdges )
find_package( OpenCV )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( FindEdges FindEdges.cpp )
target_link_libraries( FindEdges ${OpenCV_LIBS} )
